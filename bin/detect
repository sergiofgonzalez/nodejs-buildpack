#!/usr/bin/env bash

BP=$(dirname "$(dirname $0)")

PACK_STACK_ID="org.cloudfoundry.stacks.$CF_STACK" \
"$BP/bin/v3-detector" \
-buildpacks "$BP/cnbs" \
-group $1/../group.toml \
-launch "$1/.." \
-order "$BP/order.toml" \
-plan $1/../plan.toml

exit $?
